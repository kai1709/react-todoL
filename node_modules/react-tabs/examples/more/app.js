var React = require('react');
var ReactTabs	= require('../../lib/main');
var Tab	= ReactTabs.Tab;
var Tabs = ReactTabs.Tabs;
var TabList	= ReactTabs.TabList;
var TabPanel = ReactTabs.TabPanel;

var App = React.createClass({
  render: function () {
    var tabs = [];
    var panels = [];

    for (var i=65; i<=90; i++) {
      var letter = String.fromCharCode(i);
      tabs.push(<Tab key={'tab' + letter}>{letter}</Tab>);
      panels.push(<TabPanel key={'panel' + letter}>{letter}</TabPanel>);
    }

    for (var i=0; i<=9; i++) {
      tabs.push(<Tab key={'tab' + i}>{i}</Tab>);
      panels.push(<TabPanel key={'panel' + i}>{i}</TabPanel>);
    }

    return (
      <Tabs>
        <TabList>
          {tabs}
        </TabList>
        {panels}
      </Tabs>
    );
  }
});

React.render(<App/>, document.getElementById('example'));
